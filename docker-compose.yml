version: '3.8'

# Generated by SDLC Orchestrator for SCRUM-214 Integration
services:
  backend:
    build:
      context: ./.
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENVIRONMENT=development
    restart: unless-stopped
